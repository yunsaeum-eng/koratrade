Fix the character image display throughout the app. The full-body pixel art looks awkward in small chat avatars. Apply cropping to show only the upper body (face and shoulders) in most places.

AVATAR CROPPING SYSTEM:
Create a reusable CharacterAvatar component that handles cropping:

For SMALL avatars (chat bubbles, sidebar list) — show top 45% of image:
<div style={{
  width: size,
  height: size,
  borderRadius: '50%',
  overflow: 'hidden',
  flexShrink: 0,
  border: '2px solid rgba(138,101,48,0.2)'
}}>
  <img
    src={imageSrc}
    style={{
      width: '100%',
      height: '220%',
      objectFit: 'cover',
      objectPosition: 'top center',
      imageRendering: 'pixelated',
      marginTop: '0'
    }}
  />
</div>

For MEDIUM avatars (commute screen NPC cards) — show top 50% of image:
Same approach but height: '200%'

For LARGE avatars (character profiles page, onboarding selection) — show full body:
<img src={imageSrc} style={{ width: '100%', imageRendering: 'pixelated' }} />

APPLY TO THESE LOCATIONS:

1. Chat message avatars → small circular crop (top 45%)
   Size: 36x36px circle

2. Left sidebar NPC list → small circular crop (top 45%)
   Size: 34x34px circle

3. Commute screen NPC status cards → medium crop (top 50%)
   Size: 56x70px, slight rounded corners (not full circle)

4. User profile bottom of sidebar → small circular crop
   Size: 36x36px circle

5. Character profiles (/characters page) → full body
   Size: 140px wide, auto height

6. Onboarding avatar selection → full body
   Size: 100px wide, auto height

7. Chat header (top of chat area) → small circular crop
   Size: 36x36px circle

ALSO FIX — REVERT LAYOUT CHANGES:
The previous update changed the main layout in ways that look bad. Revert any layout changes that were made when applying the pixel characters. The original 3-column layout (left sidebar, center chat, right sidebar) should be exactly as it was before. Only the avatar images inside that layout should change.

ALSO FIX — MAKE IT LOOK CUTE AND POLISHED:
Add a subtle styling touch to make avatars look nicer:
- Small golden ring border around each avatar: border: 2px solid rgba(138,101,48,0.3)
- Slight hover effect on sidebar NPC items: the avatar gets a slightly brighter border on hover
- In chat messages, the avatar should have a clean white background behind it so pixel art looks crisp
