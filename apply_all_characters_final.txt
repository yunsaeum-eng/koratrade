Apply all character images from the "NPC design" folder with the following specifications.

STEP 1 — COPY FILES TO PUBLIC FOLDER
Copy all files from "NPC design" folder to public/characters/:

Full body images:
- "sophie.png" → public/characters/sophie.png
- "james.png" → public/characters/james.png  
- "min.png" → public/characters/min.png
- "sarah.png" → public/characters/sarah.png
- "박철수 과장.png" → public/characters/park.png
- "user(f).png" → public/characters/user_female.png
- "user(m).png" → public/characters/user_male.png

Cropped upper body images (파일명에 "_크롭" 붙어있음):
- "sarah_크롭.png" → public/characters/sarah_crop.png
- "james_크롭.png" → public/characters/james_crop.png
- "sophie_크롭.png" → public/characters/sophie_crop.png
- "min_크롭.png" → public/characters/min_crop.png
- "박철수 과장_크롭.png" → public/characters/park_crop.png
- "user(f)_크롭.png" → public/characters/user_female_crop.png
- "user(m)_크롭.png" → public/characters/user_male_crop.png

STEP 2 — WHERE TO USE EACH IMAGE

Full body images (전신) — use in:
- /characters page (인물 도감): show full body, width 140px, image-rendering: pixelated
- /onboarding avatar selection: show full body for user_female and user_male options

Cropped upper body images (_크롭) — use in:
- Chat message avatars (채팅창 말풍선 옆)
- Left sidebar NPC list
- Chat header (현재 대화 상대 표시)
- Commute screen NPC status cards
- User profile at bottom of left sidebar
- NPC hover tooltip popup

STEP 3 — PASTEL BACKGROUND COLORS PER CHARACTER
Based on each character's outfit colors, assign these pastel backgrounds:

Sarah Kim — wears navy/dark professional outfit:
→ background: '#EEF2F8' (soft sky blue — matches navy)
→ border accent: '#C5D5E8'

James Park — wears light blue shirt, navy trousers:
→ background: '#E8F4FF' (light powder blue)
→ border accent: '#B8D8F0'

Lisa Lee — wears white blouse, dark trousers:
→ background: '#F0F5E8' (soft sage green — clean and precise)
→ border accent: '#C8DDB0'

Min Choi — casual, younger style:
→ background: '#FFF5E8' (warm peach/cream — youthful energy)
→ border accent: '#F0D5B0'

박철수 과장 — dark practical workwear:
→ background: '#F0EEF5' (soft lavender — contrasts blunt personality)
→ border accent: '#D0C8E0'

Sophie Beaumont — elegant beige/cream French style:
→ background: '#FFF8EE' (warm ivory — matches her elegant beige outfit)
→ border accent: '#E8D8B8'

User (female) — professional outfit:
→ background: '#F5EEF8' (soft lilac)
→ border accent: '#DCC8E8'

User (male) — professional outfit:
→ background: '#EEF5F0' (soft mint green)
→ border accent: '#B8DCC0'

STEP 4 — APPLY BACKGROUNDS

In /characters page (인물 도감):
Each character card gets its assigned pastel background color.
The character full-body image sits on this colored background.
Card border uses the accent color.

In /commute page (출근 화면):
Each NPC status card gets its assigned pastel background.
Uses cropped image.

In left sidebar NPC hover tooltip:
Small popup uses the character's pastel background.

In chat message avatars:
The circular avatar container uses the character's pastel background behind the cropped image.
This makes pixel art look crisp and cute instead of plain white.

STEP 5 — AVATAR DISPLAY SETTINGS

For all cropped images in circular avatars:
width: 36px (chat) or 34px (sidebar)
height: 36px or 34px
border-radius: 50%
overflow: hidden
image-rendering: pixelated
object-fit: cover
object-position: top center
background: [character's pastel color]
border: 2px solid [character's accent color]

For full body in 인물 도감:
width: 130px
height: auto
image-rendering: pixelated
display: block
margin: 0 auto

STEP 6 — ONBOARDING AVATAR SELECTION
Show user_female.png and user_male.png as full body images.
Two cards side by side:
- Left card: user_female.png, label "여성 캐릭터", background #F5EEF8
- Right card: user_male.png, label "남성 캐릭터", background #EEF5F0
- Selected state: gold border 2px solid #8a6530
- Image size: 100px wide, auto height
- image-rendering: pixelated
